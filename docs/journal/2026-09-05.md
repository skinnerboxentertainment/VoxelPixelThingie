# 2026-09-05, Day 1, Phase 0

**Yesterday.** Nothing. The folder held SPEC.md and the scaffold, no git.

**Today.** Phase 0 stand-up, PLAN.md §2.

- Merged: repository stand-up (PR #1), CI, hooks, templates, ADRs.
- Oracle result, Verified this session:
  - Push of a commit on no PR to `main` rejected: "Changes must be made
    through a pull request. 2 of 2 required status checks are expected."
  - `ci` 16 s, `conventional title` 3 s, both pass at 4b9199f.
  - Coverage on `src/`: 96.98% lines against an 85% gate.
  - commitlint rejected "bad message no type" locally.
- Correction recorded: pushing PR #1's head commit directly to `main` was
  accepted and marked the PR merged. That is GitHub's behavior when the
  pushed commits are the head of an open PR with passing required checks.
  It is not a protection gap for unreviewed commits, which the probe above
  rejects, but it means "merge only through the button" is a habit, not a
  rule the server enforces. All later merges use `gh pr merge --squash`.
- release-please proposed 1.0.0 from the initial `feat:` commit. Fixed with
  `bump-minor-pre-major` and a 0.0.0 manifest so the release PR tracks
  0.1.0 for Phase 5.
- Dependabot opened four Action major bumps within a minute of the merge.
  Auto-merge enabled on each; they land through the same gate.

**Risk.** CodeQL default setup reported `languages=[]` at configuration
time; its first real scan runs on the next push. If it never picks up
TypeScript, switch to an explicit `codeql.yml` workflow.

**Not done.** GitHub Project board: the `gh` token lacks the `project`
scope. Milestones and labels stand in. `pages.yml` deferred to Phase 2.
