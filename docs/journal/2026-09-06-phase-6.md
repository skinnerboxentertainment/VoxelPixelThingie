# 2026-09-06, Phase 6: Persistence

**Merged.** Passport blob (#44). UUID v7 ids, container ids, frame on every
event, wrangler context (#45). Scene sink, Node file store, memory store,
openScene (#47). OPFS store, save and load, passport panel in the Three.js
demo (#48). Compaction (#49). Sink resume, attach after open, manifest ids
(#51). Read-only URL store, seal and verify, export and mirror-check
scripts, the git half of #42 (this entry's PR).

**The spime test, Verified this session.** `scripts/export-scene.ts` wrote
the reference cube, sealed, to a folder; the folder was pushed to a public
data repository; `scripts/mirror-check.ts` opened the scene from the local
folder and from the GitHub raw URL, verified both seals, and compared
digests:

| Store | Bits | Seal | Open time | Digest |
|-------|-----:|------|----------:|--------|
| local folder | 485 | ok | 287 ms | 874d9a2552f19cb9… |
| raw.githubusercontent.com | 485 | ok | 3,652 ms | 874d9a2552f19cb9… |

Same digest on both: same ids, positions, emissions, passports, links, and
render flags. The scene is at
https://github.com/skinnerboxentertainment/VoxelPixelThingie-scenes under
`scenes/reference-8`: 512 bit folders, 54,392 events, 19 MB unpacked,
1.2 MB in git.

**Per-ticket oracles, all Verified.**

- #37: three passports, one four deep, replay deep-equal; every invalid
  value throws before any event.
- #38: 10,000 ids across 10 grids unique and sorted in minting order;
  5,000 in one millisecond stay ordered past the counter wrap; wrangle
  stamps inside only, nests, restores on throw; replay stamps actor
  replay and keeps the frame.
- #39: round trip through memory and through a real folder deep-equal,
  flags included; passport.seq equals the ledger head for all 512 bits;
  torn last line discarded; 256 KiB passport refused before the bit
  changes.
- #40: carve three, set a passport, save to OPFS, reload, load: counts and
  passport back; a fresh context has no scene. 27.5 s round trip in
  headless Chromium on this machine.
- #41: compact with tail 8 then open: deep-equal to the uncompacted import
  and to the live grid; a heavily linked bit's ledger is at most 8 lines
  with no link events; manifest.compacted true.
- #50: resume, carve two more, flush, reopen: equal, no duplicated ledger
  seqs, new seqs continue from the manifest; created preserved; resume
  after compaction works.
- #42 git half: the table above; sha256 test vector matches; a tampered
  ledger and a tampered passport are both reported by name.

Unit tests 62 to 71. Coverage 98.3% lines on `src/`.

**Design decisions made in flight.**

- The sink projects a whole batch before writing anything, so a batch it
  cannot project writes nothing. Found by the resume test.
- `openScene({ attach })` attaches a sink after rebuild and the grid
  continues its sequence from the manifest, so a resumed sink sees only
  new events. The earlier `{ sink }` option still exists and receives the
  replay, which the demo's save path relies on.
- Any write drops `manifest.hashes`; sealing is a publish-time act.
- Ledger appends are batched per bit and run with bounded concurrency,
  which the browser's file system needed and the disk store did not mind.

**IPFS half of #42, Verified this session.** Pinata's free plan counts 500
files and a scene is 1,025, so the scene ships as one packed file
(`vpb-scene-pack/1`, #53). Oscar created the account and a Files-write
key, saved to a file outside the repository that the pin script reads and
never prints. `scripts/pin-pinata.ts` uploaded the 17.9 MB pack through
the v3 Files API.

- CID: `bafybeibbu7hdh6jmuovoonsppyhjzni74av6koqkq2wjemqtgjvphkwi6m`
- Gateway: https://gateway.pinata.cloud/ipfs/bafybeibbu7hdh6jmuovoonsppyhjzni74av6koqkq2wjemqtgjvphkwi6m

`scripts/mirror-check.ts` across four stores:

| Store | Bits | Seal | Open time | Digest |
|-------|-----:|------|----------:|--------|
| local folder | 485 | ok | 372 ms | 874d9a2552f19cb9… |
| GitHub raw URL | 485 | ok | 2,462 ms | 874d9a2552f19cb9… |
| local pack | 485 | ok | 170 ms | 874d9a2552f19cb9… |
| Pinata gateway, pack | 485 | ok | 159 ms | 874d9a2552f19cb9… |

The file downloaded from the gateway has the same SHA-256 as the uploaded
pack (20fad1d0a6eb17b1…). The ticket asked for the CID to match a locally
recomputed hash of the upload; a CID is IPFS's own chunked DAG hash and
recomputing it would mean reimplementing that chunker, so the check made
instead is byte equality of the round-tripped file plus the seal inside it.
The public ipfs.io gateway returned 504 at the time of writing, which is
propagation lag for a fresh pin.

**Phase 6 is complete.** v0.2.0 shipped before the IPFS step; the pack
format and pin script land in v0.3.0.
