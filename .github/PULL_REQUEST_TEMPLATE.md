## Intent

One sentence. What the linked issue asked for, and nothing else.

Closes #

## Oracle

The executable check that shows this is done, and its result at the head SHA.

```
command
result
```

## Claims audited

Every checkable sentence in this description and in the commit messages was
probed against the diff, or is marked Trusted below.

- [ ] `/intent-check` run, diff does only what the intent says
- [ ] `/roast` run, findings fixed or listed under Residual risk
- [ ] `/code-review` run
- [ ] `claim-auditor` run on this description

Trusted claims:

## Residual risk

What was not done, and what could still be wrong.
